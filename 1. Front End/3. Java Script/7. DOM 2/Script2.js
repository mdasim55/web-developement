// Here, we create a new element button.....
let newButton = document.createElement("button");
newButton.innerText="Click me!";
console.log(newButton);
/*
// Access the div.......
let div = document.querySelector("div");
// div.append(newButton);               // Add in the last of the div
// div.prepend(newButton);               // Add in the first of the div
// div.before(newButton);               // Add in the first outer the div
// div.after(newButton);               // Add in the last outer of the div

heading=document.createElement("h1");
heading.innerHTML="<i> Hello I am a new heading</i>";
document.querySelector("body").prepend(heading);

// REmove the element......
let para=document.querySelector("p");
para.remove();
heading.remove();                       // its remove the heading

// Homework --> the used of appendChild & removeChild..

*/